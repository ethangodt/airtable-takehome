{
    "name": "sql-evaluator",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "check": "./check node src/main.js -- examples",
        "check:all": "npm run check -- examples/*.sql"
    },
    "devDependencies": {
        "eslint": "6.8.0"
    },
    "dependencies": {
        "line-reader": "^0.4.0"
    }
}
